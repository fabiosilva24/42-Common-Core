#include "../include/philo.h"

int main(int argc, char **argv)
{
	
	if (argv != 5)
		printf("./Usage [Number of Program], numbers of philos, time_to_die, time_to_think, time_to_sleep, number_of_times_each_philosopher_must_eat\n");
}